import React, { useState } from "react";

const ReactWebNotifyDemo = (props) => {
	return (
		<div className="demo-header">
			<div>{props.header}</div>
            <div></div>
		</div>
	);
};

export default ReactWebNotifyDemo;
